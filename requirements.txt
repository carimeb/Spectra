# Spectra — dependências
#
# Testado com Python 3.12 e 3.13. Para rodar apenas o seed, bastam pymongo,
# python-dotenv e Faker (qualquer 3.11+); o restante atende a API e o agente.

# --- núcleo: API, banco, seed ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pymongo>=4.10.1
python-dotenv>=1.0.1
Faker>=33.1.0

# --- agente — LangGraph + integrações MongoDB/Claude ---
langgraph>=0.2.60
langchain-anthropic>=0.3.1
langchain-mongodb>=0.4.0
langgraph-checkpoint-mongodb>=0.1.0

# --- embeddings (opcional / configurável por env) ---
langchain-voyageai>=0.4.0
langchain-openai>=0.2.14
