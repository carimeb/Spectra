# Spectra — dependências
#
# IMPORTANTE: use Python 3.12. O stack do agente (Fase 4) ainda não suporta
# Python 3.13. Para rodar apenas o seed (Fase 1), bastam pymongo, python-dotenv
# e Faker — funcionam em qualquer 3.11+.

# --- núcleo: API, banco, seed (Fases 1–3) ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pymongo>=4.10.1
python-dotenv>=1.0.1
Faker>=33.1.0

# --- agente (Fase 4) — LangGraph + integrações MongoDB/Claude (requer Python 3.12) ---
langgraph>=0.2.60
langchain-anthropic>=0.3.1
langchain-mongodb>=0.4.0
langgraph-checkpoint-mongodb>=0.1.0

# --- embeddings (opcional / configurável por env) ---
langchain-voyageai>=0.4.0
langchain-openai>=0.2.14
